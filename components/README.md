# Components

This directory contains React components for the AI Financial Causal Terminal.

## Structure

- `panels/` - Main panel components (Central, Left, Right, Financial Analysis, Industry, Macro Ribbon)
- `ui/` - Reusable UI components (buttons, cards, tooltips, etc.)
- `charts/` - Chart and visualization components

Components will be implemented in subsequent tasks according to the implementation plan.
